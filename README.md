# zs-progressbar
A blocking progress bar for timed actions in FiveM. One export call runs the bar and returns true/false once it finishes or gets cancelled — no callbacks needed. Optional control locking (movement, combat, vehicle) and Esc-to-cancel support included, fully standalone.
